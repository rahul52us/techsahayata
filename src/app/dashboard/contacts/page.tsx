"use client";

import { Box, Heading, Text } from "@chakra-ui/react";

export default function ContactsPage() {
  return (
    <Box>
      <Heading as="h1" size="lg" color="teal.700" mb={2}>
        Contacts
      </Heading>
      <Text color="gray.600">Contact management is ready for the same dashboard structure.</Text>
    </Box>
  );
}
